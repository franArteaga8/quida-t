import {createTheme} from '@mui/material/styles'

export const customTheme = createTheme({
    palette: {
        // palette
    },
    spacing: {
        // spacing
    },
    typography: {
        // typography
    }
})